# Book APP

A modern, responsive book application built with React and Vite. This project showcases a sleek user interface for browsing and managing books with smooth animations and a beautiful design powered by Tailwind CSS.

## 🚀 Tech Stack

- **React 19** - A JavaScript library for building user interfaces
- **Vite** - Next generation frontend tooling for fast development and optimized builds
- **Tailwind CSS** - A utility-first CSS framework for rapid UI development
- **Motion** - Smooth animation library for engaging interactions
- **ESLint** - JavaScript linting for code quality

## 📋 Prerequisites

- Node.js (v14 or higher)
- npm or yarn package manager

## 🛠️ Installation

1. Clone the repository:

```bash
git clone https://github.com/Greycode009/Book-Discovery.git
cd "Book APP"
```

2. Install dependencies:

```bash
npm install
```

## 📝 Available Scripts

### Development Server

```bash
npm run dev
```

Starts the development server with hot module replacement (HMR). Visit `http://localhost:5173` in your browser.

### Build for Production

```bash
npm run build
```

Creates an optimized production build in the `dist` folder.

### Preview Production Build

```bash
npm run preview
```

Locally preview the production build before deployment.

### Lint Code

```bash
npm run lint
```

Runs ESLint to check code quality and style violations.

## 📁 Project Structure

```
src/
├── App.jsx              # Main application component
├── main.jsx             # Entry point
├── index.css            # Global styles
├── assets/              # Static assets (images, fonts, etc.)
├── pages/
│   └── Homepage.jsx     # Homepage component
```

## 🎯 Features

- Responsive design that works on all devices
- Smooth animations and transitions
- Clean, modern UI with Tailwind CSS
- ESLint configured for code consistency

## 🚀 Getting Started

1. Install dependencies:

```bash
npm install
```

2. Start the development server:

```bash
npm run dev
```

3. Open your browser and navigate to the URL shown in the terminal (typically `http://localhost:5173`)

4. Start editing files in `src/` to see changes automatically reflected in your browser

## 📦 Build & Deploy

To create a production build:

```bash
npm run build
```

The optimized files will be in the `dist` folder, ready for deployment.

## 🔍 Code Quality

This project uses ESLint to maintain code quality. Run the linter with:

```bash
npm run lint
```

## 📄 License

This project is available for personal use and development.

## 📧 Support

For issues, questions, or suggestions, please create an issue in the repository.
